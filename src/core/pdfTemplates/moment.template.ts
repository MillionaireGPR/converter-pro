import { PdfTemplate } from './types';

export const momentTemplate: PdfTemplate = {
  supplierId: 'moment',
  supplierName: 'Moment',
  identificationPatterns: ['MOMENT'],
  minConfidence: 30,
  
  fieldExtractors: {}
};
