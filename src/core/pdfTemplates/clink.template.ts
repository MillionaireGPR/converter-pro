import { PdfTemplate } from './types';

export const clinkTemplate: PdfTemplate = {
  supplierId: 'clink',
  supplierName: 'Clink',
  identificationPatterns: ['CLINK'],
  minConfidence: 30,
  
  fieldExtractors: {}
};
